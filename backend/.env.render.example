# ============================================================
# BACKEND - Variables de Entorno para PRODUCCIÓN (Render)
# ============================================================
# Las marcadas con [AUTO] son configuradas automáticamente
# por render.yaml. Las demás debes completarlas en:
# Render Dashboard → neosale-backend → Environment
# ============================================================

# [AUTO] Generada por Render al conectar la base de datos
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neosale"

# [AUTO] Render inyecta PORT=10000 automáticamente
# PORT=10000  ← NO la configures, Render la gestiona

NODE_ENV="production"

# URLs (Render las conecta automáticamente entre servicios via render.yaml)
CORS_ORIGIN="https://neosale-frontend.onrender.com"
FRONTEND_URL="https://neosale-frontend.onrender.com"

# --- PAGOS (Wompi) ---
WP_PUBLIC_KEY="pub_test_..."
WP_PRIVATE_KEY="prv_test_..."
WP_EVENTS="test_events_..."
WP_INTEGRITY="test_integrity_..."

# --- EMAIL (Gmail App Password) ---
# Habilitar en: myaccount.google.com/apppasswords
EMAIL_USER="tu@gmail.com"
EMAIL_PASSWORD="xxxx xxxx xxxx xxxx"
EMAIL_FROM="noreply@neosale.com"
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"

# --- ENVÍOS (EnvioClick) ---
ENVIOCLICK_API_KEY="tu-api-key"
STORE_NAME="NeoSale"
STORE_PHONE="+57 300 0000000"
STORE_ADDRESS="Tu dirección"
STORE_CITY="Tu ciudad"
